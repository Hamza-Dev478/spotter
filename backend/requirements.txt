# ELD Trip Planner — backend runtime + test dependencies (Python 3.11+)
Django==5.2.15
djangorestframework==3.16.1
django-cors-headers==4.9.0
gunicorn==23.0.0
requests==2.32.5
dj-database-url==2.3.0
whitenoise==6.12.0
psycopg[binary]==3.3.4

# Tests (safe to keep installed in prod; not imported at runtime)
pytest==8.4.2
pytest-django==4.12.0
